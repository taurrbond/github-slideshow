---
description: Review the current diff against team standards (delegates to .claude/skills/code-review)
---

Invoke the `code-review` skill defined at `.claude/skills/code-review/SKILL.md`.

Follow the checklist and output format described there. Pass through any arguments
the user provides (e.g. `--focus=liquid`, `--fix`).
