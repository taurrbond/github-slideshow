---
description: Create a new slide in _posts/ following project conventions (delegates to .claude/skills/add-slide)
---

Invoke the `add-slide` skill defined at `.claude/skills/add-slide/SKILL.md`.

Follow its steps exactly. Pass through the title and any arguments the user
provides (e.g. `--date=YYYY-MM-DD`).
